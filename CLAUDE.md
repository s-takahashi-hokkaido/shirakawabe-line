# CLAUDE.md

このリポジトリで作業するAIエージェント・開発者向けの指示。

## プロジェクトの性質

**選挙に関わるシステム**である。通常のWebアプリと決定的に違う点が3つある。

1. **公職選挙法の制約を受ける**。機能の可否が法律で決まる。実装前に必ず [docs/compliance.md](docs/compliance.md) を読むこと。
2. **やり直しが効かない**。投票日は動かない。障害や炎上が票に直結する。
3. **個人情報の機微度が高い**。支援者データは政治的信条に関わる情報である。

---

## 絶対に守ること

### 公職選挙法

- **投票日当日の選挙運動は禁止**。配信機能には必ず期間ガードを入れる。既存コンテンツの残置は可だが更新は不可。
- **事前運動は禁止**。告示日前は「政治活動」であり「選挙運動」ではない。文面のトーンが変わる。
- **表示義務**。選挙運動用の文書図画をネットで頒布する場合、連絡先の表示が必要。配信文のフッターはシステム側で強制する。
- **有料ネット広告は原則禁止**。広告連携機能を実装しない。
- **18歳未満の選挙運動は禁止**。ボランティア募集フォームには年齢確認を必ず入れる。
- **買収規制**。友だち紹介キャンペーンなど、インセンティブを伴う施策は実装しない。
- **メール/SMSでの選挙運動は要件が厳しい**（候補者・政党に限定、事前同意必須）。
  LINEは「ウェブサイト等を利用する方法」に整理されるため別枠だが、
  **メールアドレスを収集して一斉送信する機能を安易に作らない**。

判断に迷ったら実装せず、[docs/decisions.md](docs/decisions.md) に論点を追記して
人間に確認を上げること。**「たぶん大丈夫」で進めない。**

### メッセージ通数（コスト）

- **push（一斉配信・セグメント配信・ステップ配信・push API）は課金対象**
- **reply（応答メッセージ・あいさつ・リッチメニュー経由・1:1チャット）は無料**
- 新しい通知機能を提案・実装するときは、**必ず「reply で代替できないか」を先に検討する**
- push を使う実装には、コード中に理由と想定通数をコメントで残す

### 個人情報・秘密情報

- **リポジトリに以下を置かない**
  - チャネルアクセストークン、チャネルシークレット、その他の認証情報
  - 支援者の氏名・連絡先・LINEユーザーID を含む実データ
  - 後援会名簿
- 秘密情報は Cloudflare Workers Secrets / `.dev.vars`（gitignore済）で管理する
- テスト用データはすべてダミーを使う

---

## 技術スタック

| | |
|---|---|
| 言語 | TypeScript |
| フレームワーク | Hono |
| 実行環境 | Cloudflare Workers |
| DB | D1 |
| フォーム | LIFF（Phase 2） |

選定理由:常時起動不要・無料枠に収まる・Webhookの応答速度要件を満たしやすい。

### 実装方針

- Webhookは**即座に200を返し**、重い処理は `waitUntil` に逃がす（LINEのタイムアウト対策）
- 署名検証（`x-line-signature`）を必ず行う。検証なしのエンドポイントを作らない
- 配信系の処理はすべて**承認フローを経由**する。候補者または責任者の確認なしに送信できる経路を作らない
- **監査ログ**を残す（誰が・いつ・何を・何人に送ったか）
- 外部通信は失敗前提で書く。リトライとフォールバックを入れる

---

## 開発フェーズ

現在は **Phase 0（コードを書かない期間）**。

管理画面の標準機能で実現できることをコード化しない。
新機能の要望が来たら、まず「LINE Official Account Manager の標準機能でできないか」を確認する。
標準機能でできることの一覧は [docs/roadmap.md](docs/roadmap.md) にある。

Phase 1 の着手条件:標準の分析機能では流入経路が判別できなくなったとき。

---

## ドキュメントの扱い

- 管理画面の設定を変更したら、対応するdocsを更新する（設定はバージョン管理されないため）
- 未確定事項は **`TBD:` 接頭辞**を付けて [docs/decisions.md](docs/decisions.md) に集約する
- 事実確認が取れていない情報には **`要確認:`** を付ける。断定して書かない

---

## トーン・文面

- 有権者向けの文面を生成する場合、[docs/operations.md](docs/operations.md) のトーン規定に従う
- **候補者本人の発言として出力される**ことを常に意識する。政策的立場を勝手に創作しない
- 事実関係（経歴・実績・数字）を推測で書かない。素材がなければ埋めずに `TBD:` を置く

---

## 用語

| 用語 | 意味 |
|---|---|
| 告示日 | 選挙運動が可能になる日。この日から投票日前日までが選挙運動期間 |
| 政治活動 / 選挙運動 | 告示前は政治活動、告示後が選挙運動。法的な扱いが異なる |
| push / reply | 課金される配信 / 無料の応答。本プロジェクトの設計上もっとも重要な区別 |
| 認証済アカウント | 審査を通った公式アカウント。LINEアプリ内検索に表示される |
| LIFF | LINE内で動くWebアプリ。フォーム等に使う |
